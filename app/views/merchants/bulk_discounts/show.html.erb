<h1><%= "#{@merchant.name}'s Discount Details" %></h1>

<%= "Discount Quantity Threshold: #{@discount.quantity_threshold}" %>
<%= "Discount Percentage: #{@discount.discount_percent}" %>
<%= link_to "Edit This Discount", "/merchants/#{@merchant.id}/bulk_discounts/#{@discount.id}/edit" %>
